import React from 'react';

const HeroBar = () => (
  <section className="hero is-primary">
    <div className="hero-body">
      <div className="container">
        <h1 className="title">Divinity Original Sin 2 Character Planner</h1>
      </div>
    </div>
  </section>
);

export default HeroBar;